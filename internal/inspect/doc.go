// Package inspect é o estágio 4 do gateway: guardrails de inspeção de payload
// (detecção de prompt injection, redação de PII).
//
// DEFERIDO: não implementado no MVP-0. Será preenchido no épico E5, mapeando
// cada guardrail a um ID SAFE-MCP (ex.: SAFE-T1102 prompt injection). Este
// pacote existe para que cada estágio mapeie 1:1 com um pacote desde o início.
package inspect
